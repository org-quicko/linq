# linq plus Redis for the redirect cache, against an existing Postgres
# instance you already run (managed, another host, whatever). See
# docs/adr/0009 for what the Redis and in-process cache backends differ in.
#
# Set DATABASE_URL to that instance and LINQ_DEFAULT_DOMAIN, then:
#   docker compose -f docker-compose.external-postgres.with-redis.yml up
services:
  redis:
    image: redis:8
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  linq:
    build: ..
    depends_on:
      redis:
        condition: service_healthy
    environment:
      # Point this at your existing Postgres instance.
      DATABASE_URL: postgres://linq:linq@your-postgres-host:5432/linq
      LINQ_REDIS_URL: redis://redis:6379
      LINQ_DEFAULT_DOMAIN: ${LINQ_DEFAULT_DOMAIN:-localhost:${LINQ_PORT:-3000}}
      # An admin key is minted and printed on first boot; read it from
      # `docker compose logs linq`. For another without restarting:
      #   docker compose exec linq bun run key:create --name ops --role admin
      # Logs go to stdout and to /data/logs/linq.log. Client IPs are never
      # recorded in either; see docs/adr/0003.
      LINQ_LOG_LEVEL: info
    ports:
      - "${LINQ_PORT:-3000}:3000"
    volumes:
      - linq-data:/data

volumes:
  linq-data:
